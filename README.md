# LinguaListen Frontend (Vite + React)

## Quickstart (Local)
```bash
npm install
npm run dev
# open http://localhost:5173
```

## Configure API
Create `.env` with:
```
VITE_API_BASE_URL=http://127.0.0.1:8000
```
or set it to your Render URL after backend deploy.